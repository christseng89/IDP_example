{
  "name": "idp-backstage",
  "version": "1.0.0",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build:backend": "yarn workspace backend build",
    "tsc": "tsc"
  },
  "devDependencies": {
    "@backstage/cli": "*",
    "typescript": "~5.3.0"
  }
}
